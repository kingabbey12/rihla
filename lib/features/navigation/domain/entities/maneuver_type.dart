/// Turn-by-turn maneuver types supported by the maneuver engine.
enum ManeuverType {
  continueStraight,
  turnLeft,
  turnRight,
  slightLeft,
  slightRight,
  uTurn,
  merge,
  exit,
  roundabout,
  arrive,
}
